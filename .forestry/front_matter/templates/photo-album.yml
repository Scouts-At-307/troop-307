---
label: Photo Album
hide_body: true
fields:
- name: name
  type: text
  config:
    required: true
  label: Name
- name: cover_image
  type: file
  config:
    maxSize: 64
  label: Cover Image
  description: Image that is used as the cover image for the album.
- name: photos
  type: image_gallery
  config: {}
  label: Photos
  description: Upload photos for the album here.
pages:
- content/albums/test.md
